server:
  port: 8080
  ssl:
    key-store: 'classpath:/etc/cas/thekeystore'
    key-store-password: changeit
#    enabled: false
cas:
  service-registry:
    json:
      location: 'classpath:/etc/cas/services'
      watcher-enabled: true
  authn:
    pm:
      enabled: true
      reset:
        crypto:
          enabled: false
      core:
        enabled: false
#        password-policy-pattern: ^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[$@$!%*?&])[A-Za-z\d$@$!%*?&]{8,10}
#        webflow:
#          enabled: true
    mfa:
      simple:
        token:
          core:
            time-to-kill-in-seconds: "60"
            token-length: "6"
        name: mfa-simple
        order: '0'
        mail:
          priority: 1
          text: Hello! Your requested CAS token is ${token}
          subject: "[ReCAP] CAS login token"
          attribute-name: email
      triggers:
        global:
          global-provider-id: mfa-simple
    accept:
      users: null
    jdbc:
      'query[0]':
        sql: SELECT password FROM users WHERE username=? and active=1
        url: jdbc:mysql://scsb-mysql:3306/recapcas?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
        dialect: org.hibernate.dialect.MySQLDialect
        user: tststage2
        password: 7279ab68-cba6-4216-991a-f555f3d1bcac
        ddlAuto: none
        driverClass: com.mysql.cj.jdbc.Driver
        field-password: password
        password-encoder:
          type: DEFAULT
          encoding-algorithm: MD5
          character-encoding: UTF-8
          secret: recap
  webflow:
    auto-configuration:
      order: '0'
      enabled: 'true'
  logout:
    followServiceRedirects: true
  httpWebRequest:
    header:
      xframe: true
      xss: true

spring:
  mail:
    host: smtp.gmail.com
    from: dinakarn31@gmail.com
    subject: "[ReCAP] CAS login token"
    sqlQuery: "SELECT email FROM users WHERE username = ?"
    username: dinakarn31@gmail.com
    protocol: smtp
    password: nuytahahczippwot
    port: 587
    testConnection: true
    default-encoding: UTF-8
    jndi-name: mail/Session
    properties:
      mail:
        smtp:
          auth: 'true'
          enable: 'true'
          starttls:
            enable: 'true'
  datasource:
    username: tststage2
    url: jdbc:mysql://scsb-mysql:3306/recapcas?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    password: 7279ab68-cba6-4216-991a-f555f3d1bcac
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  thymeleaf:
    enabled: true
    cache: true
    check-template: true
    check-template-location: true
    spring-el-compiler: false
    encoding: UTF-8
    mode: HTML

logging:
  level:
    org:
      apereo:
        cas: DEBUG
      springframework:
        security: DEBUG

